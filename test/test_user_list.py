"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.pagination import Pagination
from ibutsu_client.models.user import User
from ibutsu_client.models.user_list import UserList


class TestUserList:
    """UserList model tests"""

    def test_creation_empty(self):
        model = UserList()
        assert model.users is None
        assert model.pagination is None

    def test_creation_full(self):
        item = User(email="e1")
        pagination = Pagination(page=1)
        model = UserList(users=[item], pagination=pagination)
        assert len(model.users) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = User(email="e1")
        model = UserList(users=[item])
        data = model.to_dict()
        assert "users" in data
        assert len(data["users"]) == 1

    def test_to_dict_with_pagination(self):
        item = User(email="e1")
        pagination = Pagination(page=1)
        model = UserList(users=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = User(email="e1")
        model = UserList(users=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["users"]) == 1

    def test_from_dict(self):
        item_dict = {"email": "e1"}
        data = {"users": [item_dict], "pagination": {"page": 2}}
        model = UserList.from_dict(data)
        assert len(model.users) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"users": [{"email": "e1"}], "pagination": {"page": 3}}'
        model = UserList.from_json(json_str)
        assert len(model.users) == 1

    def test_multiple_items(self):
        i1 = User(email="e1")
        i2 = User(email="e2")
        model = UserList(users=[i1, i2])
        data = model.to_dict()
        assert len(data["users"]) == 2

    def test_roundtrip_json(self):
        item = User(email="e1", id=uuid4())
        original = UserList(users=[item])
        json_str = original.to_json()
        restored = UserList.from_json(json_str)
        assert len(restored.users) == 1
        assert restored.users[0].id == original.users[0].id

    def test_to_str(self):
        model = UserList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = UserList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = User(email="e1")
        orig = UserList(users=[item])
        model = UserList.from_dict(orig)
        assert len(model.users) == 1

    def test_list_none_values_excluded(self):
        model = UserList(users=[])
        data = model.to_dict()
        assert "users" in data
        assert "pagination" not in data
