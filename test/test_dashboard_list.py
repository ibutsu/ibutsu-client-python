"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.dashboard import Dashboard
from ibutsu_client.models.dashboard_list import DashboardList
from ibutsu_client.models.pagination import Pagination


class TestDashboardList:
    """DashboardList model tests"""

    def test_creation_empty(self):
        model = DashboardList()
        assert model.dashboards is None
        assert model.pagination is None

    def test_creation_full(self):
        item = Dashboard(title="d1")
        pagination = Pagination(page=1)
        model = DashboardList(dashboards=[item], pagination=pagination)
        assert len(model.dashboards) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = Dashboard(title="d1")
        model = DashboardList(dashboards=[item])
        data = model.to_dict()
        assert "dashboards" in data
        assert len(data["dashboards"]) == 1
        assert data["dashboards"][0]["title"] == "d1"

    def test_to_dict_with_pagination(self):
        item = Dashboard(title="d1")
        pagination = Pagination(page=1)
        model = DashboardList(dashboards=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = Dashboard(title="d1")
        model = DashboardList(dashboards=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["dashboards"]) == 1

    def test_from_dict(self):
        item_dict = {"title": "d1"}
        data = {"dashboards": [item_dict], "pagination": {"page": 2}}
        model = DashboardList.from_dict(data)
        assert len(model.dashboards) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"dashboards": [{"title": "d1"}], "pagination": {"page": 3}}'
        model = DashboardList.from_json(json_str)
        assert len(model.dashboards) == 1

    def test_multiple_items(self):
        i1 = Dashboard(title="d1")
        i2 = Dashboard(title="d2")
        model = DashboardList(dashboards=[i1, i2])
        data = model.to_dict()
        assert len(data["dashboards"]) == 2

    def test_roundtrip_json(self):
        item = Dashboard(title="d1", id=uuid4())
        original = DashboardList(dashboards=[item])
        json_str = original.to_json()
        restored = DashboardList.from_json(json_str)
        assert len(restored.dashboards) == 1
        assert restored.dashboards[0].id == original.dashboards[0].id

    def test_to_str(self):
        model = DashboardList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = DashboardList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = Dashboard(title="d1")
        orig = DashboardList(dashboards=[item])
        model = DashboardList.from_dict(orig)
        assert len(model.dashboards) == 1

    def test_list_none_values_excluded(self):
        model = DashboardList(dashboards=[])
        data = model.to_dict()
        assert "dashboards" in data
        assert "pagination" not in data
