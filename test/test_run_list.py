"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.pagination import Pagination
from ibutsu_client.models.run import Run
from ibutsu_client.models.run_list import RunList


class TestRunList:
    """RunList model tests"""

    def test_creation_empty(self):
        model = RunList()
        assert model.runs is None
        assert model.pagination is None

    def test_creation_full(self):
        item = Run(source="s1")
        pagination = Pagination(page=1)
        model = RunList(runs=[item], pagination=pagination)
        assert len(model.runs) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = Run(source="s1")
        model = RunList(runs=[item])
        data = model.to_dict()
        assert "runs" in data
        assert len(data["runs"]) == 1

    def test_to_dict_with_pagination(self):
        item = Run(source="s1")
        pagination = Pagination(page=1)
        model = RunList(runs=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = Run(source="s1")
        model = RunList(runs=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["runs"]) == 1

    def test_from_dict(self):
        item_dict = {"source": "s1"}
        data = {"runs": [item_dict], "pagination": {"page": 2}}
        model = RunList.from_dict(data)
        assert len(model.runs) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"runs": [{"source": "s1"}], "pagination": {"page": 3}}'
        model = RunList.from_json(json_str)
        assert len(model.runs) == 1

    def test_multiple_items(self):
        i1 = Run(source="s1")
        i2 = Run(source="s2")
        model = RunList(runs=[i1, i2])
        data = model.to_dict()
        assert len(data["runs"]) == 2

    def test_roundtrip_json(self):
        item = Run(source="s1", id=uuid4())
        original = RunList(runs=[item])
        json_str = original.to_json()
        restored = RunList.from_json(json_str)
        assert len(restored.runs) == 1
        assert restored.runs[0].id == original.runs[0].id

    def test_to_str(self):
        model = RunList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = RunList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = Run(source="s1")
        orig = RunList(runs=[item])
        model = RunList.from_dict(orig)
        assert len(model.runs) == 1

    def test_list_none_values_excluded(self):
        model = RunList(runs=[])
        data = model.to_dict()
        assert "runs" in data
        assert "pagination" not in data
