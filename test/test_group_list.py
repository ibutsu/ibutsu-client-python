"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.group import Group
from ibutsu_client.models.group_list import GroupList
from ibutsu_client.models.pagination import Pagination


class TestGroupList:
    """GroupList model tests"""

    def test_creation_empty(self):
        model = GroupList()
        assert model.groups is None
        assert model.pagination is None

    def test_creation_full(self):
        item = Group(name="g1")
        pagination = Pagination(page=1)
        model = GroupList(groups=[item], pagination=pagination)
        assert len(model.groups) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = Group(name="g1")
        model = GroupList(groups=[item])
        data = model.to_dict()
        assert "groups" in data
        assert len(data["groups"]) == 1

    def test_to_dict_with_pagination(self):
        item = Group(name="g1")
        pagination = Pagination(page=1)
        model = GroupList(groups=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = Group(name="g1")
        model = GroupList(groups=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["groups"]) == 1

    def test_from_dict(self):
        item_dict = {"name": "g1"}
        data = {"groups": [item_dict], "pagination": {"page": 2}}
        model = GroupList.from_dict(data)
        assert len(model.groups) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"groups": [{"name": "g1"}], "pagination": {"page": 3}}'
        model = GroupList.from_json(json_str)
        assert len(model.groups) == 1

    def test_multiple_items(self):
        i1 = Group(name="g1")
        i2 = Group(name="g2")
        model = GroupList(groups=[i1, i2])
        data = model.to_dict()
        assert len(data["groups"]) == 2

    def test_roundtrip_json(self):
        item = Group(name="g1", id=uuid4())
        original = GroupList(groups=[item])
        json_str = original.to_json()
        restored = GroupList.from_json(json_str)
        assert len(restored.groups) == 1
        assert restored.groups[0].id == original.groups[0].id

    def test_to_str(self):
        model = GroupList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = GroupList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = Group(name="g1")
        orig = GroupList(groups=[item])
        model = GroupList.from_dict(orig)
        assert len(model.groups) == 1

    def test_list_none_values_excluded(self):
        model = GroupList(groups=[])
        data = model.to_dict()
        assert "groups" in data
        assert "pagination" not in data
