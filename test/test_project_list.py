"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.pagination import Pagination
from ibutsu_client.models.project import Project
from ibutsu_client.models.project_list import ProjectList


class TestProjectList:
    """ProjectList model tests"""

    def test_creation_empty(self):
        model = ProjectList()
        assert model.projects is None
        assert model.pagination is None

    def test_creation_full(self):
        item = Project(name="p1")
        pagination = Pagination(page=1)
        model = ProjectList(projects=[item], pagination=pagination)
        assert len(model.projects) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = Project(name="p1")
        model = ProjectList(projects=[item])
        data = model.to_dict()
        assert "projects" in data
        assert len(data["projects"]) == 1

    def test_to_dict_with_pagination(self):
        item = Project(name="p1")
        pagination = Pagination(page=1)
        model = ProjectList(projects=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = Project(name="p1")
        model = ProjectList(projects=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["projects"]) == 1

    def test_from_dict(self):
        item_dict = {"name": "p1"}
        data = {"projects": [item_dict], "pagination": {"page": 2}}
        model = ProjectList.from_dict(data)
        assert len(model.projects) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"projects": [{"name": "p1"}], "pagination": {"page": 3}}'
        model = ProjectList.from_json(json_str)
        assert len(model.projects) == 1

    def test_multiple_items(self):
        i1 = Project(name="p1")
        i2 = Project(name="p2")
        model = ProjectList(projects=[i1, i2])
        data = model.to_dict()
        assert len(data["projects"]) == 2

    def test_roundtrip_json(self):
        item = Project(name="p1", id=uuid4())
        original = ProjectList(projects=[item])
        json_str = original.to_json()
        restored = ProjectList.from_json(json_str)
        assert len(restored.projects) == 1
        assert restored.projects[0].id == original.projects[0].id

    def test_to_str(self):
        model = ProjectList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = ProjectList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = Project(name="p1")
        orig = ProjectList(projects=[item])
        model = ProjectList.from_dict(orig)
        assert len(model.projects) == 1

    def test_list_none_values_excluded(self):
        model = ProjectList(projects=[])
        data = model.to_dict()
        assert "projects" in data
        assert "pagination" not in data
