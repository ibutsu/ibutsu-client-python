"""
Ibutsu API

A system to store and query test results

The version of the OpenAPI document: 2.8.3
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
from uuid import uuid4

from ibutsu_client.models.pagination import Pagination
from ibutsu_client.models.result import Result
from ibutsu_client.models.result_list import ResultList


class TestResultList:
    """ResultList model tests"""

    def test_creation_empty(self):
        model = ResultList()
        assert model.results is None
        assert model.pagination is None

    def test_creation_full(self):
        item = Result(test_id="t1")
        pagination = Pagination(page=1)
        model = ResultList(results=[item], pagination=pagination)
        assert len(model.results) == 1
        assert model.pagination.page == 1

    def test_to_dict(self):
        item = Result(test_id="t1")
        model = ResultList(results=[item])
        data = model.to_dict()
        assert "results" in data
        assert len(data["results"]) == 1

    def test_to_dict_with_pagination(self):
        item = Result(test_id="t1")
        pagination = Pagination(page=1)
        model = ResultList(results=[item], pagination=pagination)
        data = model.to_dict()
        assert "pagination" in data
        assert data["pagination"]["page"] == 1

    def test_to_json(self):
        item = Result(test_id="t1")
        model = ResultList(results=[item])
        json_str = model.to_json()
        data = json.loads(json_str)
        assert len(data["results"]) == 1

    def test_from_dict(self):
        item_dict = {"test_id": "t1"}
        data = {"results": [item_dict], "pagination": {"page": 2}}
        model = ResultList.from_dict(data)
        assert len(model.results) == 1
        assert model.pagination.page == 2

    def test_from_json(self):
        json_str = '{"results": [{"test_id": "t1"}], "pagination": {"page": 3}}'
        model = ResultList.from_json(json_str)
        assert len(model.results) == 1

    def test_multiple_items(self):
        i1 = Result(test_id="t1")
        i2 = Result(test_id="t2")
        model = ResultList(results=[i1, i2])
        data = model.to_dict()
        assert len(data["results"]) == 2

    def test_roundtrip_json(self):
        item = Result(test_id="t1", id=uuid4())
        original = ResultList(results=[item])
        json_str = original.to_json()
        restored = ResultList.from_json(json_str)
        assert len(restored.results) == 1
        assert restored.results[0].id == original.results[0].id

    def test_to_str(self):
        model = ResultList()
        s = model.to_str()
        assert isinstance(s, str)

    def test_from_dict_none(self):
        model = ResultList.from_dict(None)
        assert model is None

    def test_from_dict_non_dict(self):
        item = Result(test_id="t1")
        orig = ResultList(results=[item])
        model = ResultList.from_dict(orig)
        assert len(model.results) == 1

    def test_list_none_values_excluded(self):
        model = ResultList(results=[])
        data = model.to_dict()
        assert "results" in data
        assert "pagination" not in data
